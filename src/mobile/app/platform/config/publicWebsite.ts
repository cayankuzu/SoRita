/**
 * The public website. It hosts the share handoff page (lists/), the download
 * page and the auth callbacks, and Supabase names the same address as its
 * site_url, which a test keeps in step. Shared links point here so they open
 * as ordinary HTTPS links in WhatsApp or Instagram.
 */
export const PUBLIC_WEBSITE_URL = 'https://cayankuzu.github.io/SoRita_web';
